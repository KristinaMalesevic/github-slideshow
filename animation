<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Animacija</title>
        <meta name="viewport" width="device-width, initial-scale=1.0">
        <style>

        body{
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #050210;
        }
        .circle{
            position: relative;
            width: 500px;
            height: 500px;
            border-radius: 50%;
            background: linear-gradient(#fb5dad, #55fb9f, #b97aff);
            animation: animate 1s linear infinite;
        }   
        @keyframes animate {
             0%{transform: rotate(0deg);} 
             100%{transform: rotate(360deg);} 
        }
        .circle span{
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(#fb5dad, #55fb9f, #b97aff);
        }
     
        .circle span:nth-child(1){
            filter: blur(50px);
        }
    
        .circle span:nth-child(2)::after{
            content: '';
            position: absolute;
            top: 30px;
            left: 30px;
            right: 30px;
            bottom: 30px;
            background:  #050210;
            border-radius: 50%;
        }

        #emty::before{
            content: '';
            position: absolute;
            top: 30px;
            left: 30px;
            right: 30px;
            bottom: 30px;
            background:  #0e0a25;
            border-radius: 50%;
            animation: animate2 1s ease-out infinite;
            
        }

        @keyframes animate2 {
            0%{ 
                transform: scale(0);
            }
            100%{
                transform:scale(1);
            }
        }
        

        </style>

    </head>

    <body>
        <div class="circle">
            <span></span>
            <span></span>
            <div id="emty"></div>
        </div>


    </body>

</html>
